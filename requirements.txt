# Core dependencies for tcrconsensus
pandas>=1.5
numpy>=1.23
click>=8.0
pyyaml>=6.0
networkx>=2.8
scipy>=1.9
scikit-learn>=1.1
matplotlib>=3.6

# Optional clusterer backends — see README for install details.
# Do NOT uncomment here; several are not pip-installable from PyPI.
#   tcrdist3:    pip install ".[tcrdist3]"
#   DeepTCR:     see README two-step workflow (pinned deps)
#   clusTCR:     pip install --no-deps "clustcr @ git+https://github.com/svalkiers/clusTCR.git"
#   GLIPH2:      tcrconsensus install-backends --gliph2
#   GIANA:       tcrconsensus install-backends --giana
#   TCRMatch:    tcrconsensus install-backends --tcrmatch

# Development
pytest>=7.0
